'use client'

import { Button, Container, Table, Form, Alert } from 'react-bootstrap'

function Error() {
  return (
    <>
      <Alert variant="danger">Gagal memuat data</Alert>
    </>
  )
}

export default Error